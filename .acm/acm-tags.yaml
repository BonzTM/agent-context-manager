version: acm.tags.v1
canonical_tags:
  acm: [acm, agent-context-manager, context-manager]
  context: [context, receipt, receipts, fetch]
  work: [work, plan, plans, task, tasks, tracking]
  verify: [verify, verification, verify_tests, verify:tests]
  init: [init, bootstrap, onboard, onboarding]
  sync: [sync, indexing, index]
  health: [health, inventory-health, health-check, health_check, health-fix, health_fix, fixer, unindexed]
  contract: [contract, contracts, payload, envelope]
  schema: [schema, schemas, json-schema, cli.command.schema, cli.result.schema, shared.schema]
  cli: [cli, command, commands, flags, convenience]
  mcp: [mcp, tool, tools, tooling-surface, acm-mcp]
  adapters: [adapter, adapters]
  postgres: [postgres, pg, pgsql]
  sqlite: [sqlite, wal, shm]
  migrations: [migration, migrations, ddl]
  runtime: [runtime, config, env, service_factory]
  workspace: [workspace, root-detection, dotenv, gitignore]
  logging: [logging, logs, log-level, log-sink]
  docs: [docs, documentation, readme, adr, concepts, getting-started]
  skills: [skills, broker, codex, claude, slash-commands]
  ci: [ci, workflow, workflows, github-actions, artifact]
  tests: [test, tests, testing, unit, integration]
  storage: [storage, repository, persistence]
  parity: [parity, consistency, drift]
